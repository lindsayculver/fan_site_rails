  <%= form_tag(gears_path, :method => "get", class: "navbar-form navbar-left", role: "search", id: "search") do %>
    <div class="input-append">
      <%= text_field_tag :search, params[:search], id: "search", class: "span2 form-control", placeholder: "Search Gear" %>

      <button class="btn btn-default" type="submit">Search</button>
    </div>
  <% end %>
  
